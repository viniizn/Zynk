{
    "build": {
      "buildCommand": "composer install && npm install && npm run build",
      "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
    },
    "deploy": {
      "restart": true
    },
    "variables": {
      "APP_ENV": "production",
      "APP_DEBUG": "false",
      "APP_URL": "https://https://zynk-production.up.railway.app//"
    }
  }
  